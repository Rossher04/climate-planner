import 'package:flutter/material.dart';

class AppColors {
  // Paleta fria oficial del proyecto.
  // Primario #4A90E2 - Secundario #2C6E91 - Acento #B8DFF5
  // Fondo #F5F7FA - Texto #2D3748
  static const ink = Color(0xFF2D3748); // Texto principal
  static const muted = Color(0xFF6B7C93); // Texto secundario (gris frio)
  static const line = Color(0xFFD9E2EC); // Bordes / divisores
  static const background = Color(0xFFF5F7FA); // Fondo
  static const blue = Color(0xFF4A90E2); // Primario
  static const navy = Color(0xFF2C6E91); // Secundario
  static const accent = Color(0xFFB8DFF5); // Acento
  // Colores de estado dentro de la familia fria (sin tonos calidos).
  static const green = Color(0xFF2E8B82); // Exito / recomendado (teal)
  static const gold = Color(0xFF2C8FB3); // Revisar (cian)
  static const coral = Color(0xFF5C6BC0); // Alerta / reagendar (indigo)
}
